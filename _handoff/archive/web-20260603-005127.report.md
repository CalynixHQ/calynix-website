---
id: web-20260603-005127
scope: web
status: done
reported: 2026-06-03
---
**Input:** Direct user requests on calynix.com (no HQ-dispatched inbox task): publish Fibby's privacy policy, update Fibby game info, add per-game detail pages with screenshots + app icon, add a site-wide header (logo on every page) + Games nav, merge contact into the footer with social links, and a series of layout/spacing/branding refinements.

**Output (all built, verified, deployed to main → Vercel, confirmed live):**
- **Fibby privacy policy** live at https://calynix.com/fibby/privacy (from FIBBY/legal source).
- **Games section is now a reusable, data-driven pattern** (`app/games/games.ts` is the single source of truth): the `/games` grid + clickable `/games/[slug]` detail pages with full description, app icon, and screenshot gallery. Adding a future game = one data entry + assets in `public/games/<slug>/`.
- **Fibby**: detail page at `/games/fibby` with 4 optimized screenshots + the App Store app icon; info matched to current status ("Submitted to the App Store — coming soon" — Waiting for Review per FIBBY scope; iPhone & iPad; free; Fibonacci number puzzle).
- **Reveal**: detail page exists with description; no screenshots/icon yet (placeholders/fallback shown).
- **Site chrome**: sticky header with the Calynix wordmark (links home) on every page + a "GAMES" nav rendered as a matching wordmark (brand triangle as the "A", Avenir Next Demi Bold); active-page highlight. Footer now carries company info, site/legal nav, and Contact (info@calynix.com + Instagram @calynix, YouTube @calynix, X @calynixhq, TikTok @calynixhq). Unified content width site-wide; homepage hero centered; spacing/alignment cleaned up.
- Earlier this session (already in main): brand assets wired (favicons/PWA/OG), footer company number 17245007, content matched to company structure.

**For HQ memory (summary only — do not copy code detail):**
- calynix.com now publicly shows: Calynix Games (Fibby, Reveal) with per-game pages; Fibby's store-facing status is "submitted, awaiting review"; public socials linked = IG/YouTube/X/TikTok (note: IG still parked per HQ record). BestMostLast remains absent from public site by design.
- Fibby App Store privacy-policy URL requirement is satisfied (live page).

**Open items / blockers:**
- When **Fibby** is approved & live: provide the App Store URL → web will set `storeUrl` (lights up a store button) and flip the status.
- **Reveal** screenshots + app icon: pending a source folder → then wired the same way.
- Decisions still owned by HQ (not web): NyleStyle repurpose by 2026-07-31; GitHub `calynixhq` not linked on site (not requested).
