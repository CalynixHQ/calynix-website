---
id: web-20260603-185130
scope: web
status: done
reported: 2026-06-03
---
**Input:** (1) Add a `/fibby` landing/support page mirroring the new `/reveal`
page. (2) Catch-up report for the rest of this session's web changes not yet
covered by an outbox report.

**Output — /fibby landing page (deployed, commit eef47f3):**
- New static page `app/fibby/page.tsx`, structurally identical to `/reveal`:
  Hero (icon + "Fibby" + tagline + App Store button + "Free · No ads · Optional
  in-app purchases") → About → Features (🔢 Fibonacci Puzzle / 📐 Three Board
  Sizes / 🏆 Game Center Leaderboards / ⚡ Quick to Learn / 🔒 Privacy-Friendly)
  → Screenshot gallery → Support (info@calynix.com + Privacy link) → footer
  "© 2026 Calynix Ltd."
- Fibby has no Terms page, so only Privacy (`/fibby/privacy`) is linked — matched
  to what exists rather than inventing a page.
- App Store button is a non-clickable "Coming soon to the App Store" label
  (Fibby submitted 2026-06-02, auto-release on approval, no public URL yet).
  A single `APP_STORE_URL` constant at the top of the file flips it to a live
  "Download on the App Store →" link when the URL is known.
- Assets copied into `public/fibby/` (icon + 4 screenshots, WebP). Fibby's
  `/games` card now points at `/fibby` via the `href` override on game data.
- Facts sourced from the FIBBY scope's ship report (iOS-only, Free, no ads,
  submitted 2026-06-02 under Calynix Ltd).

**Output — other web changes this session (all deployed to main):**
- `/reveal` landing/support page + Reveal legal pages `/reveal/privacy` &
  `/reveal/terms` (already detailed in report web-20260603-182816).
- Per-game **Legal section** added to the `/games/[slug]` template
  (data-driven `privacyUrl`/`termsUrl`; uniform for all current/future games).
- Reveal **game detail** fleshed out: icon + 5 screenshots wired into games.ts.
- Removed "BestMostLast" mention from `/privacy` (no longer named on any
  public webpage; only internal _handoff/ notes still reference it).
- UI tuning: header/logos to 0.8x; header highlight lighter + bluer (#1B2E55);
  content container widened to 96rem; hero subtitle one row at 0.8x; footer
  sizing; Calynix wordmark viewBox centered (equal L/R gaps); GAMES wordmark
  M↔A gap widened.

**Verification (live, www.calynix.com — apex 307→www is normal):** /fibby,
/fibby/privacy, /reveal, /reveal/privacy, /reveal/terms, /games, /privacy all
return 200. /games links to both /fibby and /reveal. Required copy present on
both landing pages.

**Open items / blockers:**
- Fibby approved → set `APP_STORE_URL` in `app/fibby/page.tsx` (button →
  "Download on the App Store") and flip Fibby status copy in `games.ts`.
- Reveal approved → same on `app/reveal/page.tsx` (URL already wired:
  apps.apple.com/app/id6776199155) + flip Reveal status.
- Calynix Games wordmark asset copied to public/reveal/ but unused on the page
  (available if HQ wants the studio mark shown).
